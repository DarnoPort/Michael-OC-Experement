name: Build Michael OS

on:
  push:
    branches:
      - "phase-*"
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-multilib nasm binutils grub-pc-bin grub-common xorriso

      - name: Check host tools
        run: python3 -m py_compile tools/diskfs_host.py tools/test_diskfs_host.py

      - name: Test host DiskFS and ELF tools
        run: python3 tools/test_diskfs_host.py

      - name: Build kernel
        run: make clean && make

      - name: Check Multiboot image
        run: make check

      - name: Build ISO
        run: make iso
